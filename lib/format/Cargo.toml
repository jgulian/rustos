[package]
name = "format"
version = "0.1.0"
edition = "2021"

[dependencies]
shim = { path = "../shim" }
format_derive = { path = "./format_derive", optional = true }

[features]
default = ["derive"]
no_std = ["shim/no_std"]
alloc = []
derive = ["format_derive"]