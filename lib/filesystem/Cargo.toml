[package]
name = "filesystem"
version = "0.1.0"
edition = "2021"

[dependencies]
shim = { path = "../shim", features = ["alloc"] }
format = { path = "../format", features = ["alloc"] }
log = "0.4" #FIXME: remove

[features]
no_std = ["shim/no_std", "format/no_std"]